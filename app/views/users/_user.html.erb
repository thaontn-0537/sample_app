<li>
  <%= gravatar_for user, size: Settings.gravatar.sm %>
  <%= link_to user.name, user %>
  <% if can_destroy_user? user %>
  | <%= link_to "delete", user, data: {"turbo-method": :delete, turbo_confirm: 
    t(".you_sure?")}%>
  <% end %>
</li>
